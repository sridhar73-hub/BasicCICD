name: CI Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4  # upgraded to v4

      - name: Debug - List files
        run: ls -la  # confirms CICD.sh exists in repo root

      - name: Fix line endings & Run CICD Script
        run: |
          sed -i 's/\r//' ./CICD.sh
          chmod +x ./CICD.sh
          bash ./CICD.sh

      - name: Publish Testsigma Test Results
        uses: mikepenz/action-junit-report@v4
        if: always()
        with:
          report_paths: '**/junit-report-*.xml'
          fail_on_failure: true
          require_tests: true
